package me.dupreez.thesisPoC.redesign.utils;

import java.io.File;

public class Constants {
    public static final String COLLECTION_PATH =
            System.getProperty("user.dir") + File.separator + "data" + File.separator + "collection" + File.separator;
    public static final String RESULTS_PATH =
            System.getProperty("user.dir") + File.separator + "data" + File.separator + "results" + File.separator;
    public static final String REFERENCE_SOLUTIONS_PATH =
            System.getProperty("user.dir") + File.separator + "data" + File.separator + "referenceSolutions" + File.separator;
    public static final String PLOTS_PATH =
            System.getProperty("user.dir") + File.separator + "data" + File.separator + "plots" + File.separator;
}
